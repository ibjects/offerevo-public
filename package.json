{
  "name": "offerevo-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html privacy.html terms.html sitemap.html sitemap.xml robots.txt llms.txt styles.css script.js mark.svg CNAME BingSiteAuth.xml dist/ && cp -R images dist/"
  }
}
